package com.example.demo.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class LoginController {
	/*
	 * @GetMapping("/authenticate") public ResponseEntity<AuthenticationBean>
	 * login() { return new ResponseEntity<AuthenticationBean>(HttpStatus.OK) ; }
	 */
}
